services:
  board:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./queue:/app/queue
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
